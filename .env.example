# Edge
EDGE_PORT=8080

# Postgres
POSTGRES_PORT=5432
POSTGRES_SUPERPASS=postgres

# Databases + service users
CORE_DB=core_db
CORE_DB_USER=core_user
CORE_DB_PASSWORD=core_password

STUDY_DB=study_db
STUDY_DB_USER=study_user
STUDY_DB_PASSWORD=study_password

# JWT shared settings
JWT_ISSUER=lms-core
JWT_AUDIENCE=lms-clients
JWT_SIGNING_KEY=dev-secret-key-change-me